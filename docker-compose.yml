version: '3.8'

services:
  api:
    build: .
    container_name: dev-doc-api
    ports:
      - "5001:5000"
    volumes:
      - ./data:/usr/src/app/data
    environment:
      - PORT=5000
      - JWT_SECRET=${JWT_SECRET:-dev-doc-secret-key-123}
      - NODE_ENV=production
    restart: always
